# Entscheidungen und Prozesswissen

> Architekturentscheidungen, offene Entscheidungen, technische Schulden und Learnings.

## Architekturentscheidungen (final)

| ID | Entscheidung |
|---|---|
| E-01 | Vanilla JS (kein Framework) |
| E-02 | D3.js v7 fuer alle Visualisierungen |
| E-03 | Kein Build-Tool, direkte ES6-Module auf GitHub Pages |
| E-04 | Leaflet + CartoDB (deferred) |
| E-05 | Offline-first (alle Daten bei Startup) |
| E-06 | Google Sheets als Erfassungstool |
| E-07 | Wikidata Q-IDs als Normdaten |
| E-08 | JSON-LD / RiC-O 1.1 als Datenformat |
| E-09 | Mobilitaetsform-Praefix `[mobilitaet:]` im Anmerkungsfeld |
| E-10 | Synthetische Daten mit `_meta.synthetic` |
| E-11 | 4 D3-Vis (Matrix, Kosmos, Zeitfluss, Mobilitaet) + 3 Views (Archiv, Indizes, Korb) |
| E-12 | Netzwerk-Schwellenwert 3+ fuer Matrix |
| E-13 | 5-Jahres-Intervalle in Matrix |
| E-14 | Komponisten-Farbkodierung |
| E-15 | 7 Lebensphasen LP1–LP7 |
| E-16 | Scroll-Morphing im Kosmos |
| E-17 | Farbe + Linienstil fuer Mobilitaetstypen |
| E-18 | Tab-basiert (Archiv, Indizes, Mobilitaet, Korb, Matrix, Kosmos) |
| E-19 | Bestand/Chronik-Toggle mit Inline-Expansion |
| E-20 | Inline-Expansion (Archiv) + Slide-in Sidebar (deaktiviert) |
| E-21 | Collapsible Provenienz-Darstellung |
| E-22 | Gruppierte Verknuepfungen mit Icons |
| E-23 | Horizontale Toolbar fuer Visualisierungen |
| E-24 | Export CSV > JSON-LD > GEXF > GeoJSON |
| E-25 | Keine praeemptive Performance-Optimierung |
| E-26 | Seiten statt Modals — About/Projekt/Hilfe als eigenstaendige HTML-Seiten |
| E-27 | 4 funktionale Farbkategorien: KUG-Blau (Interaktion), Signal-Gruen (Verknuepfung), Neutral-Grau (Abwesenheit), Warmer Hintergrund (Struktur) |
| E-28 | Handreichung als UX-Quelle — Erfassungskonventionen direkt in Frontend-Texte |
| E-29 | Dynamischer Counter: "X von Y Objekten" bei aktivem Filter |
| E-30 | Stats-Bar entfernt — Info nur kontextuell im Archiv-Tab |
| E-31 | `m3gim:hasAssociatedAgent` statt `rico:hasOrHadAgent` (existiert nicht in RiC-O 1.1) |
| E-32 | Erwaehnungen als `rico:hasOrHadSubject` mit `@type: rico:Person` (standard RiC-O statt custom `m3gim:mentions`) |
| E-33 | `m3gim:eventDate` fuer Datum-Literale (rico:isAssociatedWithDate ist ObjectProperty) |
| E-34 | `@context`-Aliase (`name` → `rico:name`, `role` → `m3gim:role`, `komponist` → `m3gim:komponist`) |
| E-35 | GitHub Actions Workflow entfernt — Pipeline laeuft lokal |
| E-36 | Floating-Tooltip (HTML-div ueber SVG) statt CSS-`::after` — SVG-Elemente unterstuetzen keine Pseudo-Elemente |
| E-37 | Popup-Menue fuer Multi-Dokument-Dots — bei >1 Dokumenten pro Gastspiel-Dot |
| E-38 | Guest-City-Display-Normalisierung via GUEST_DISPLAY_MAP |
| E-39 | Piecewise-linear Zeitskala mit Skalenbruch (BREAK_YEAR=1975, BREAK_RATIO=0.74) |
| E-40 | Excel-Quelldateien in `data/source/` git-getrackt (Reproduzierbarkeit statt nur Google Sheets) |
| E-41 | Layer-Toggle statt gleichzeitiger Darstellung aller Schichten in Mobilitaet (5 Layer: mobilitaet, auftritte, netzwerk, repertoire, sparkline) |
| E-42 | Vertikale Event-Marker statt Bezier-Pfeile fuer Mobilitaetsereignisse — Position + Text statt Kurve + Farbe |
| E-43 | `extract_auftritte()` Pipeline-Funktion — 3-Pass-Extraktion (strukturiert, Programmhefte/Plakate, Rezensionen) mit 4 Kategorien (engagement, festspiel, gastspiel, konzert) |
| E-44 | `buildLayerChips()` als Multi-Select-Erweiterung des Phase-Chip-Patterns in viz-components.js |
| E-45 | Auftritte-Aggregation — Deduplizierung nach (ort, jahr, werk), Dots ueber dem Balken positioniert |
| E-46 | `kontext`-Felder in `mobilitaet[]` — narrative Zusatztexte fuer Forschungsbezug (z.B. Netzwerk-Deltas, Wissenstransfer) |
| E-47 | Prototyp-Seiten als eigenstaendige HTML (lebensstationen.html, lebenspartitur.html) statt weitere SPA-Tab-Iterationen |
| E-48 | Lebensstationen: Scrollytelling mit IntersectionObserver, 7 Kapitel + 7 Wendepunkte, Sticky Mini-Timeline |
| E-49 | Lebenspartitur: Vertikaler Bump-Chart (Lebenslinie + synchronisierte Facetten fuer Netzwerk/Repertoire) |
| E-50 | DEV/Prod Log-Toggle: `viewLog()` gibt No-Ops auf GitHub Pages, Console-Diagnostik nur auf localhost |
| E-51 | Error Boundaries pro View: main.js faengt Render-Fehler pro Tab ab (sync+async), erlaubt Re-Render |
| E-52 | Deutsche Fehlermeldungen in loader.js: Netzwerk/404/Parse unterschieden, `loadPartitur()` warnt statt stilles null |
| E-53 | Zentraler Cross-View Event-Bus (`events.js`): `onViewNavigate(tab, handler)` mit Auto-Replay statt `pendingHighlight`-Pattern pro View |
| E-54 | Prototyp-Seiten modularisiert: inline CSS/JS extrahiert in externe Module, `loadPartitur()` Singleton statt direktem fetch() |
| E-55 | ARIA-Accessibility: `role="tablist/tab/tabpanel"`, `aria-selected` dynamisch, `aria-hidden` auf dekorativen SVG-Icons |
| E-56 | Responsive Breakpoints: `@media <768px` in base.css + components.css, FF-Badges hidden, Toolbars kompakter |
| E-57 | Lebenspartitur als SPA-Tab: `renderLebenspartitur(store, container)` mit container-relativem DOM, Standalone-`init()` beibehalten fuer `lebenspartitur.html` |
| E-58 | Fuzzy-Matching in reconcile.py: `thefuzz.token_set_ratio`, 3 Confidence-Level (exact/fuzzy_high/fuzzy_low), backward-kompatibles JSON-Format |
| E-59 | Wikidata-Enrichment-Pipeline: `enrich-wikidata.py` holt Properties (P106, P412, P569/P570, P625, P1191 etc.), `transform.py` injiziert `owl:sameAs` + `m3gim:`-Properties in JSON-LD |
| E-60 | UA-Distanz im Kosmos: Phase-Filter-Annotation zeigt durchschnittliche Urauffuehrungsdistanz, Werk-Tooltips zeigen individuelle Distanz |
| E-61 | Indizes-Subtitles: `renderNameCell()` zeigt Beruf, Stimmfach, Lebensdaten aus WD-Enrichment unter Personennamen |
| E-62 | ~~v2-Parallelstruktur~~ — **obsolet seit 2026-04-17, Session 29**: v2 wurde zum alleinigen Default konsolidiert. `data/google-spreadsheet/` + `data/output/` + `data/reports/` sind die einzigen operativen Verzeichnisse; alte v1-Stände liegen unter `data/_archive/`. ENV-Overrides bleiben als Technik verfügbar (z.B. für Experimente mit alternativen Datenständen), sind im Normalbetrieb aber nicht mehr nötig. `build-views.py` kopiert im Default-Lauf automatisch `m3gim.jsonld` + Derivate nach `docs/data/`. |
| E-63 | Gender-neutrale Rollennormalisierung (`normalize_role`): `:in`/`:innen`-Suffixe werden gestrippt. Ambiguierende End-`in`-Formen bleiben erhalten (interpret, technische leitung). Kontrolliertes Vokabular in [datenmodell.md § 5](datenmodell.md). |
| E-64 | SKOS-Hierarchie für Dokumenttypen: `build_dft_concepts()` emittiert `skos:Concept`-Knoten mit `skos:broader` als Top-Level-Graph-Entitäten. Korrespondenz, Presse, Programm, Biographisch, Identitätsdokument als Oberklassen. Nur effektiv verwendete Konzepte werden emittiert (transitives Closure). |
| E-65 | `m3gim:dateEvidence` → `agrelon:hasProvenance` + `agrelon:hasConfidenceValue`: Der Record verweist als Provenance auf sich selbst; die Evidenz-Stufen (`aus_dokument`/`erschlossen`/`extern`/`unbekannt`) werden auf Dezimal-Konfidenzen (1.0/0.6/0.8/0.0) gemappt. Kompletter Drop des alten Property-Namens, nicht additiv — **Frontend-breaking**, Phase 6 nachziehen. |
| E-66 | `m3gim:SpatiotemporalEvent` als Top-Level-Graph-Entitäten: Komposit `ort, datum` erzeugt zusätzlich zu den zwei getrennten Relationen eine SpatiotemporalEvent-Instanz mit `atPlace`, `atDate`, `eventRole`. Records referenzieren via `m3gim:hasSpatiotemporalEvent`. Rein additiv, bestehende `rico:hasOrHadLocation`+`m3gim:eventDate` bleiben. |
| E-67 | Finanzschicht strukturiert: `parse_monetary_value` zerlegt `AMOUNT, CURRENCY`, Komma als Dezimaltrenner. `m3gim:DetailAnnotation` trägt `monetaryAmount` (xsd:decimal), `currency` (belegter Code ohne ISO-4217-Zwang wegen Ambiguität „Fr" = FRF/CHF), `detailRole`, `detailValue` (Rohwert). |
| E-68 | Typisierte Datumsproperty-Familie: `m3gim:absendedatum`, `m3gim:auffuehrungsdatum`, `m3gim:premieredatum` etc. je nach Rolle statt generisch `m3gim:eventDate`. `is_iso_date()` als Gatekeeper gegen Freitext-Leaks (`"Wien, ab 1956"` landet im Fallback `m3gim:eventDate`, nicht in typisierter Property). `clean_date` normalisiert `YYYY-YYYY` → `YYYY/YYYY` (data.md § 6). |
| E-69 | AgRelOn für Agent-Agent-Relationen: `AGRELON_MAPPING` (typ, rolle) → AgRelOn-Klasse + -Property. Umgesetzt: `HasEmployeeEmployer`, `HasCorrespondent`, `HasProfessionalContact`, `HasIsPatron`, `HasIsMember`. Emission als `m3gim:agentRelation`-Array am Record mit `agrelon:hasProvenance` (Record-URI). `hasValidityPeriod` aus `rico:date` als Heuristik für Employer-Relationen. |
| E-70 | v2-Konsolidierung (Session 29, 2026-04-17): v2 ist alleiniger Default, v1 unter `data/_archive/` archiviert. `docs/data/m3gim.jsonld` ist alleinige primäre Datenquelle des Frontends — Derivate (`partitur.json`, `matrix.json`, `kosmos.json`) sind optionale Visualisierungs-Hilfen, die vom Store aus regeneriert werden können. `build-views.py` kopiert seitdem auch `m3gim.jsonld` automatisch nach `docs/data/` (zuvor manueller Schritt, Drift-Quelle). E-62 damit obsolet. |
| E-71 | `FINANCE_CURRENCY_DEFAULTS` pro Archivsignatur-Präfix in `transform.py`: greift, wenn XLSX-Typ `ausgaben, währung` markiert ist, das Namensfeld aber keinen Währungs-Suffix trägt. Aktuell `UAKUG/NIM_007` → `S` (Schilling). Defaults sind explizite Redaktionsentscheidungen; ohne Eintrag bleibt `currency` leer und `validate.py` warnt. Alternative heuristisches Raten im Parser wurde bewusst verworfen, weil historisch korrekte Währungszuordnung inhaltlich, nicht syntaktisch erfolgen muss. |
| E-72 | Phase 6 — Store-Maps in `loader.js` (Session 30): `dftHierarchy`, `mobilityEvents`+`recordToEvents`, `agentRelations`, `finances`, plus typisierte Datumsfelder als Fallback in `indexByYear`. Jede Map ist eigenständige Indexer-Funktion, die Pass 1 (Top-Level-Knoten: Concepts, STE) bzw. Pass 2 (record-gebunden: events-refs, agentRelations, finances) durchläuft. Pass 1.5 verdrahtet `skos:broader` zu `children[]`-Backrefs. DFT-Concepts und STE werden **nicht** direkt als Record-Properties indexiert — sie sind Top-Level-Graph-Entitäten mit `@id`, und Records verweisen via `@id`. Die Views sehen die Maps erst nach Phase 7 (View-Migration). |
| E-73 | `m3gim:xlsxSource` als technische Provenance pro Record und pro aus Verknüpfungen abgeleiteter Entität (DetailAnnotation, AgRelOn-Relation, SpatiotemporalEvent). Blank-Node mit `xlsxSheet` (`"Objekte"` / `"Verknuepfungen"`), `xlsxRow` (1-basiert inkl. Header) und optional `datenpunktId`. Einzelne Record-Properties (`rico:title` etc.) bekommen keinen eigenen Eintrag — ihre Quelle ist die des umgebenden Records. Ergänzt die semantische Provenance (`agrelon:hasProvenance`) und ermöglicht direkte Rücknavigation XLSX-Zelle → JSON-LD → UI-Chip. Pipeline-Implementation in [transform.py](../scripts/transform.py) `convert_objekt` + `process_verknuepfungen` + `add_relations_to_records`. Kontrakt in [datenmodell.md § 9](datenmodell.md). |
| E-74 | Low-Confidence-Policy: Reconcile-Matches mit `match = "fuzzy_low"` (Score 80–89) werden **nicht automatisch** ins Enrichment oder in die JSON-LD übernommen. Filter in `enrich-wikidata.py` + `transform.py` lässt nur `exact`, `fuzzy_high` und explizit als `manual_review: "approved"` freigegebene Einträge durch. Der Quality-Snapshot listet die low-conf-Einträge zur redaktionellen Freigabe. Begründung: Fehl-Matches wie „Schottland" → „Scotland" (Score 89) würden sonst ungeprüft Wikidata-Properties in die Personenindex-Subtitles kippen. Konservative Policy hebt die WD-Coverage langsamer, schützt aber Datenintegrität. |
| E-75 | Interface-Redesign als Forschungswerkzeug, nicht Dashboard (Session 32). Die sechs D3-Prototypen (Mobilität, Matrix, Kosmos, Zeitfluss, Lebenspartitur, Lebensstationen) werden entfernt, ihre Lektionen in [frontend.md § Lektionen](frontend.md) konserviert. Neue Tab-Architektur: Archiv, Biogramm, Mobilitäts-Atlas, Netzwerk, Repertoire — Perspektiven auf denselben Graph, kein Feature-Wechsel. (Der ursprünglich mitgeplante Erschließungsstand-Tab wurde in Session 33 wieder entfernt, weil der Markdown-Report `data/reports/quality-snapshot.md` die Team-Qualitätssicht ausreichend trägt.) Designregeln und Muster ([interface-konzept.md](interface-konzept.md)) binden: Source-Serif-Titel + Mono-Signatur + warmer Papier-Hintergrund, Rolle-Prefix-Chips als universelles Daten-Atom (Einzelbeleg + Aggregat-Breakdown mit gleicher Primitive), Confidence als Micro-Dot, Provenance-Pille pro Datenpunkt, Selection per Kontur, Tabelle-vor-Chart für Rankings, Datenqualität wird gezeigt (nicht gemergt), minimalistische Interaktions-UI statt Toolbar-Chrome. Tab-Namen inhaltlich ohne „View"/„Chart"-Floskeln. Begründung: Die sechs Prototypen waren je für sich brauchbar, aber uneinheitlich in Designsprache, überladen in Interaktion und schlecht gekoppelt an die Phase-6-Store-Maps. Ein einheitliches, daten-getreueres Interface ist für die FWF-Bewerbung und für die Erschließungsarbeit wertvoller als sechs weiterentwickelte Einzel-Viz. |
| E-76 | Leaflet + OpenStreetMap als Karten-Basis für den Mobilitäts-Atlas (Session 33). CDN-Load ohne API-Key, Canvas-Renderer (`preferCanvas: true`) für Performance bei Marker-Clustern. Alternative MapLibre/D3-Geo wären aufwendiger (GeoJSON-Hosting, keine Tiles); Leaflet ist pragmatisch und gut dokumentiert. Marker-Größe skaliert mit Event-Zahl pro Ort, Signal-Grün markiert Auswahl. Voraussetzung ist der Koordinaten-Patch (STE.atPlace trägt `geo:lat`/`geo:long` via `_inject_enrichment`). Kein Marker-Cluster-Plugin im MVP (Datenvolumen klein genug); Option offen für späte Phase. |
| E-77 | Funktionale Gruppierung im Archiv-Inline-Detail (Session 34). Sechs typ-basierte Sektionen (Personen · Institutionen · Orte · Werke · Rollen · Erwähnt) werden zu fünf funktionalen Blöcken: **Produktion** (Komponist, Librettist, Dirigent, Regisseur, Bühnenbildner …), **Mitwirkende** (Sänger, Interpret, Adressat, Vermittler …), **Werk & Repertoire** (`MusicalWork` + `hasPerformanceRole` gemeinsam), **Ort & Ereignis** (Places + SpatiotemporalEvents zusammengeführt, dedupliziert), **Erwähnt** (rolle=erwähnt). Nicht-mapped Rollen landen in einem Default-Bucket „Weitere", nicht stumm in einer Fehlkategorie. Mapping in `ROLE_TO_SECTION` + `sectionForRole()` in `docs/js/data/constants.js`. `renderEntityChips()` entfällt, alle Chips laufen jetzt durch `buildRoleChip()` mit Cluster-Farbe und Provenance-Pille. |
| E-78 | Manuelle Q-ID-Approvals brauchen Live-Label-Verifikation (Session 34). Aus dem Session-34-Batch haben zwei von zehn Q-IDs nicht gepasst: Bayreuth Q2861 war Rostock, Stanislau Q200491 war ein US-Videospiel-Publisher. Ohne Browser-Verifikation (Atlas-Marker in Oberfranken statt an der Ostsee, Country-Label „Vereinigte Staaten" für Iwano-Frankiwsk) wären die Fehler nicht aufgefallen. Neues Helper-Skript `scripts/verify-manual-approvals.py` prüft jeden `match: "manual"`-Eintrag gegen Live-Wikidata (Label + Aliases + Typ-Signal in Description). Exitcode 1 bei Mismatch → geeignet als Pre-Commit- oder CI-Gate. Offline-Bypass via `SKIP_VERIFY_MANUAL=1`. Als Pflichtlauf nach Approval-Batches in [CLAUDE.md](../CLAUDE.md) verankert. |
| E-79 | Smarter P17-Claim-Selector in `enrich-wikidata.py` (Session 34). Berlin Q64 hat elf historische P17-Claims (Mark Brandenburg, Deutsches Reich, DDR, BRD …). Das frühere `values[0]` griff den chronologisch ersten (Mark Brandenburg) statt den aktuell gültigen (Deutschland Q183). Der neue Helper `_pick_current_claim()` wählt nach fester Reihenfolge: (1) `rank == "preferred"` (Wikidatas kuratierter Default), (2) Claim ohne `P582` (end time) Qualifier — also noch gültig, (3) erster Non-deprecated-Claim. Angewendet auf Entity-Ref-Properties P19/P20/P17/P276/P86. Löst Berlin → Deutschland ohne Manual-Override. Zugleich: Label-Resolve auch für bereits gecachte Entitäten (vorher blieben Q-IDs aus alten Cache-Läufen stumm als „Q39"-Strings sichtbar). |
| E-80 | Tab-Zählung konsistent: sechs Perspektiv-Tabs + ein Werkzeug-Tab (Session 34). Live-Code hat sieben Tabs (`TABS` in router.js): Archiv, Indizes, Mobilitäts-Atlas, Repertoire, Biogramm, Netzwerk, Korb. Das interface-konzept führt sie als „sechs Perspektiven auf denselben Graph" + Wissenskorb als Querschnitts-Werkzeug. Alte Formulierungen „fünf Tabs" bzw. „Dossier" (Mockup-Wort) werden auf das Code-Vokabular (Archiv, sechs Perspektiven) angeglichen. Begründung: ein Dokument, das die Live-Implementierung beschreibt, nutzt auch deren Vokabular — sonst entstehen stumme Such-Mismatches beim Navigieren Mockup→Code. |
| E-81 | Tabs temporaer reduziert auf Bestand / Chronik / Indizes (spaeter + Statistik, + Wissenskorb via E-86). Die vier Perspektiv-Tabs (Mobilitäts-Atlas, Repertoire, Biogramm, Netzwerk) sind weiterhin per `hidden`-Attribut unsichtbar; `TAB_RENDERERS`, CSS und Hash-Routing bleiben aktiv, Bookmarks auf versteckte Tabs werden in `router.parseHash` auf Bestand umgebogen. Begründung: Fokus-Interface, solange Perspektiv-Tabs nicht fertig ueberarbeitet sind. Reaktivieren = `hidden`-Attribut in `docs/index.html` entfernen + `VISIBLE_TABS`-Set in `router.js` erweitern. |
| E-82 | Konvolut-Metadaten direkt in der Bestand-Zeile statt als aufklappbares Detail. Top-3-Dokumenttyp-Chips + Status-Mix (abgeschlossen / begonnen / zurueckgestellt) werden unter dem Konvolut-Titel gerendert. `buildKonvolutDetail` in `archiv-inline-detail.js` ist entfernt. Begruendung: (1) Konvolut-Click hatte Kollision mit Inline-Detail-Toggle (User-Bug), (2) Metadaten wurden bisher erst nach Klick sichtbar -- direkt-sichtbare Aggregate sind informativer bei gleichen Kosten. Aggregation passiert im Loader (Pass 3), nicht on-the-fly. |
| E-83 | Hierarchische Sortierung: Konvolute bleiben Signatur-sortiert, Kinder *innerhalb* ihres Konvoluts werden nach dem gewaehlten Sort-Key sortiert. Frueheres Verhalten (flacher Sort) hat die archivische Hierarchie zerrissen (Konvolute und Kinder wurden durcheinander gemischt). Bei aktivem Filter wird weiterhin flach sortiert, da der Filter die Hierarchie ohnehin aufloest. |
| E-85 | Statistik-Tab ist Showroom, nicht Research-Tool (Session 37). Der neue vierte Tab zwischen Chronik und Indizes aggregiert den gesamten Bestand zu einer visuellen Zusammenschau — Hero-Kennzahlen, Dokumenttypen, Bearbeitungsstand, Mobilitätssichten, Geografie, Netzwerk, Repertoire, Verlinkung, Finanzen. Er ist bewusst **read-only**: keine Toolbar, keine Filter, keine internen Module-Variablen, keine Re-Renders. Begründung: (1) DH-Projekte brauchen eine Eingangs-Sicht, die zeigt was die Daten *sind* und was damit *möglich* wird, nicht was man im Einzelnen damit recherchieren kann — Forschungsfragen wandern in die Fach-Tabs und in zukünftige dedizierte Perspektiv-Tabs (Mobilitäts-Atlas, Repertoire, Biogramm, Netzwerk). (2) Die strikte Read-Only-Natur senkt die Bug-Oberfläche gegenüber Bestand/Chronik deutlich — keine `currentFilters`-Silent-Regression-Äquivalente möglich. (3) Statistik wird auf Knowledge-Ebene in `knowledge/frontend.md` verankert, nicht als eigenes Spec-Dokument — der Tab ist Frontend-Verhalten, kein Modell. Ablehnungen: Filter/Drill-Down (macht Bestand/Chronik obsolet), Vergleichsmodi (Research-Tool-Feature), Exporte (Wissenskorb-Feature). |
| E-92 | Session 45 — Statistik schaerfer, Chronik dichte-adaptiv. (1) **Statistik-Hero entfernt**: die vier Kennzahl-Kacheln (`buildHeroRow`/`heroCard` in `statistik.js`, `.statistik-hero*` in `statistik.css`) sind raus \u2014 sie waren Tech-Dashboard-Residuum und duplizierten die Zahlen, die in den Sektions-Diagrammen ohnehin als Legenden/Totals auftauchen. (2) **Mobilitaetssichten als Balken-Chart** statt Feature-Kacheln: die sechs Sichten (plus "Nicht klassifiziert") rendern jetzt als einziger horizontaler `buildHorizontalBars`-Chart mit eigener Farbfamilie pro Sicht (`SICHT_COLOR`), sortiert nach Haeufigkeit. Die alten Karten (`.statistik-sicht*` + `statistik-sicht__example`-Links) sind weg \u2014 die "Beispiel:"-Links waren redaktionelle Anekdoten (kollidiert mit E-87) und die Dichte-Mini-Balken verglichen nur tile-intern statt quer ueber Sichten. Der Balken-Chart macht die Proportionen zwischen Sichten direkt lesbar. (3) **Histogramm mit Achsentiteln**: `buildHistogram` akzeptiert `xAxisLabel`/`yAxisLabel`, Geografie-Jahrzehnt-Histogramm zeigt `Jahrzehnt` / `Anzahl Events`. Neue `.stat-axis__title`-Regel. Margins leicht groesser (left 36\u219252, bottom 30\u219246) damit die Titel Platz haben. (4) **Chronik dichte-adaptive Jahresh\u00f6he**: leere Jahre ohne Dekaden-Status rendern als 6-px-Linie (Label versteckt, Marker 6 px hoch), belegte Jahre bleiben bei voller Hoehe. Dekaden-Jahre (% 10 == 0) bleiben auch bei Null Records sichtbar als Anker, damit die Lueckenstruktur weiter lesbar ist (E-88 "Datenqualitaet wird gezeigt"). Kein neues DOM, kein Toggle \u2014 pure CSS-Regel `.chronik-year--empty:not(.chronik-year--decade)`. |
| E-91 | Session 44 Finalisierung: (1) Chip-Klick setzt Toolbar-Filter statt in den Index zu navigieren. `applyArchivFilter(facet, value)` in `router.js` dispatcht `m3gim:navigate` an den aktiven Record-Tab (Bestand oder Chronik), der seine `toolbar.setLocation/setWerk/setPerson` aufruft. `buildRoleChip`-Callsites im Inline-Detail und im Korb nutzen `chipClickFor(gridType, name)` mit Fallback auf `navigateToIndex` fuer Grids ohne Facet-Equivalent (Organisationen). Tip-Text einheitlich "Als Filter setzen". (2) **Indizes-Toolbar** migriert auf die generische `buildToolbar`-Komponente: globale Suche ueber alle vier Grids + neues `Nur mit Wikidata`-Toggle-Facet. Per-Grid-Search (`gridState.*.search`) entfernt, Cross-Grid-Facet-Filter (`activeFilter`) bleibt als separate Ebene. Tote CSS-Regeln `.idx-toolbar` + `.idx-search-input` weg, neue `.archiv-toggle`-Regel fuer das Toggle-Control. (3) **Refactor extractXlsxSource**: Drei identische Implementierungen in `loader.js`, `archiv-inline-detail.js`, `korb.js` durch `utils/provenance.js` ersetzt \u2014 eine Quelle, stricteres Verhalten (null wenn keine Zeile), Konsumenten bleiben unveraendert. (4) **Korb-Follow-ups entschieden**: Konvolute bookmarkbar = Nein (eigene Export-Aggregationslogik waere noetig, bleibt offen); Tab-Name bleibt "Wissenskorb" (im interface-konzept.md festgehalten, Projektvokabular). (5) **Smoke-Test** auf Chronik-Scroll-Zeitstrahl umgestellt: `chronik-year`+`chronik-point` statt `chronik-period`+`chronik-record`, logStamp-Erwartungen (`records, jahre-belegt, undatiert, spanne`), Click-Canary prueft Sprung aus Chronik-Punkt ins Bestand-Inline-Detail. |
| E-90 | UI-Feinschliff Session 43: drei nebeneinander liegende UI-Defekte entfernt. (1) **Konvolut-Aufklapp-Affordance**: `.konvolut-toggle` war 20 \u00d7 20 mit 12 \u00d7 12 SVG in `--color-text-secondary` ohne Hintergrund \u2014 visuell kaum als Button erkennbar. Jetzt 22 \u00d7 22 mit 14 \u00d7 14 SVG in `--color-kug-blau`, leichtem `rgba(0,74,143,0.08)`-Hintergrund, Hover-Verstaerkung, Row-Hover ueberhebt den Toggle zusaetzlich. `aria-expanded`/`aria-label` am Button. (2) **Provenance-Tooltip**: Die Pille im `buildRoleChip()` nutzte natives HTML-`title` bei `#3`, der umgebende Chip hatte parallel ein CSS-`data-tip` \u2014 bei Hover auf die Pille erschienen beide Tooltips. Fix: Pille auf das CSS-`data-tip`-System umgestellt (mehrzeilig via `tipWrap`, mit Sheet + Zeile + ggf. Datenpunkt-ID), Label von `#3` auf `Z.3` + Paperclip-Icon, und der Chip-Level-Tip (`tip: 'Im Index oeffnen'`) wird nur noch gesetzt, wenn die Chip-Children keine eigenen Tips tragen. Damit nur ein Tooltip pro Hover. (3) **Wikidata-Badge als Link**: `badge--wikidata` innerhalb `chip--role-pair` hat jetzt eine linke Trennlinie (`border-left`), 12 \u00d7 12 SVG statt 10 \u00d7 10, eigenen Hover-Hintergrund und eigenes `data-tip="Bei Wikidata ansehen"`. `stopPropagation` im Click-Handler bleibt, damit Wikidata-Klick nicht den Chip-Klick ausloest. (4) **Statistik-Feinschliff**: Donut-Legenden-Labels bekommen `data-tip` mit dem Voll-Text (Ellipsis haelt schmal, Tooltip zeigt alles), Histogramm-X-Achse duennt Ticks bei > 10 Kategorien auf jedes zweite Label aus, `.stat-chart__svg-wrap` erlaubt Shrinking fuer schmale Viewports. Chip-Klick-Action bleibt zunaechst `navigateToIndex` (siehe Plan, Phase C). |
| E-89 | Statistik-Tab als visuelles Portr\u00e4t statt Tech-Report (Session 42). Sieben Sektionen wurden zu sechs: `Bearbeitungsstand`-Balken und `Verlinkung & Qualit\u00e4t` komplett entfernt, `Dokumenttypen` als D3-Donut mit Legende, `Netzwerk` als Donut (AgRelOn) + Bar (Kategorien), `Finanzen` als Donut (W\u00e4hrungen) + Bar (Rollen), `Geografie` erweitert um Events-pro-Jahrzehnt-Histogramm. Zwei neue Vis-Primitives in `views/statistik.js`: `buildDonut` und `buildHistogram`, beide mit `buildHorizontalBars`-Fallback. D3 v7 global via CDN (schon fuer `mobilitaets-atlas` eingebunden). Begruendung: E-85 hielt Statistik als Showroom fest, aber die urspruengliche Umsetzung war zahlenlastig (Prozent-Balken, Abdeckungs-Tabellen). Die neuen Diagramme zeigen *was* die Daten sind, nicht *wie vollstaendig* sie sind; Tech-Reporting (Bearbeitungsstand, WD-Coverage, Low-Confidence-Policy) lebt im Markdown-Report `data/reports/quality-snapshot.md`, wo es ohnehin hingehoert. Statistik bleibt strikt read-only (keine Toolbar). Tote CSS-Regeln (`.statistik__caveat`, `.statistik-chip*`, `.statistik-span*`, `.statistik-note`, `.stat-bars__fill--*`) mit entfernt. |
| E-88 | Chronik als scrollender Jahres-Zeitstrahl statt Perioden-Akkordeon (Session 41). Jedes Jahr 1919–2009 (+ Ausreisser) erhaelt eine eigene Zeile: Jahres-Label links, Dot auf der Zeitachse, Records dieses Jahres als klickbare Chips rechts. Dot-Groesse skaliert mit Jahresbelegung (Dichte als Form), leere Jahre bleiben als Umriss-Dot sichtbar. Gruppierungs-Toggle (Ort/Person/Werk) und Fuenfjahresperioden-Buckets entfallen; der Perioden-Summary `topTypes · topGroups` faellt damit ebenfalls weg. Toolbar-Filter (Suche, Typ, Person, Ort, Werk) greifen unveraendert auf die Record-Menge. Begruendung: der lineare Scroll-Flow ist fuer zeitliche Lektuere natuerlicher als Akkordeon-Navigation, die Jahres-Gliederung ist feinkoerniger als Fuenfjahres-Buckets und macht Erschliessungsluecken ohne redaktionelle Labels (vgl. E-87) sichtbar. Rangiert als Prototyp — spaetere Feinschliffe (Scrubber, Dichte-Heatmap, Zoom) sind offen. |
| E-87 | Keine redaktionellen Deutungen im UI — handverdrahtete Karriere-Notizen pro Chronik-Periode (`KARRIERE_NOTIZEN` in `archiv-chronik.js` mit Labels wie „Internationale Karriere", „Späte Karriere") entfernt, inkl. Editorial-Marker-Badge (`.chronik-period__note--editorial`, `.chronik-period__editorial-marker` in `archiv.css`) und Session-36-Interface-Konzept-Passage „Redaktionelle Einordnung wird markiert". Begründung: Promptotyping-Grundsatz „Documents as Source of Truth, UI als Daten-Spiegel" schließt interpretative Labels aus, die nicht aus den Metadaten ableitbar sind — das `red.`-Badge signalisiert zwar Transparenz, normalisiert aber zugleich die Mischung aus Daten und Deutung im UI. Datengetriebene Periodencharakterisierung läuft weiter über `summaryText` (Top-Dokumenttypen · Top-Gruppen aus `store.records` pro Periode). Ausnahmen, die bleiben: strukturelle Labels (`1950-1954`, `Undatiert`, „ohne Datumsangabe in der Quelle"), prozessuale Begriffe in der Low-Confidence-Policy („auf redaktionelle Sichtung" in `statistik.js`). |
| E-93 | Netzwerk-Tab als konzentrische Personen-Visualisierung um Malaniuk (Session 46). Die frühere Pivot-Tabelle über `store.agentRelations` zeigte nur ~13 Relationen zu 6 Gegenparteien und blendete die Wagner-Familie, Strauss, Mozart und die 72 Multi-Record-Personen aus — das entsprach nicht mehr der Forschungsfrage „Mit welchen Personen stand Malaniuk in Beziehung?". Neu: Malaniuk im Zentrum, alle Personen aus `store.persons` auf **zwei** konzentrischen Ringen. Ring 1 = harte Beziehung (AgRelOn-Partner ODER Wikidata + ≥ 5 Dokumente), Ring 2 = wiederkehrendes Umfeld (≥ 2 Dokumente). Ring 3 (einmalige Nennungen) wurde verworfen — reiner dekorativer Halo. Winkel alphabetisch, Positionen analytisch aus Sinus/Kosinus (frontend.md § 196 — Determinismus vor Force-Simulation). Rolle wandert in die Füllfarbe über `derivePersonKategorie()`, das aus `entry.roles`-Sets ableitet (Produktion > Bühne > Vermittlung > Korrespondenz > Presse > Erwähnt) — ersetzt die statische Namens-Keyword-Kategorie, die nur ~70 von 305 Personen traf. **Zwei Linientypen explizit unterschieden** (der eigentliche Nutzer-Aha nach Live-Test): gerade blaue Radials zum Zentrum = `agrelon:*`-Beziehungen, **explizit** in den Metadaten annotiert (aus `store.agentRelations`); geschwungene Bezier-Bänder = **Ko-Okkurrenz**, automatisch aus Paaren von Personen-in-denselben-Dokumenten abgeleitet. Beide Linien tragen native SVG-`<title>`-Tooltips mit dem *Grund* der Verbindung (AgRelOn-Typ bzw. geteilte Dok.-Anzahl) und haben getrennte Sichtbarkeits-Toggle. Click pinnt den Highlight-Zustand (Kontur + Drop-Shadow, Nachbarn beschriftet, Rest gedimmt); Filter ändern nur Opazität, nicht Position, damit der „groß anfangen, dann verdichten"-Flow keinen Layout-Sprung hat. Sidebar mit Slidern, Zeitfenster (Von/Bis, baut Person-→-Jahres-Index), Kategorie-Chips und Legende (immer ausgeklappt, mit expliziten Linien-Swatches). Zoom/Pan via `d3.zoom()`. Geometrie in neuem Pure-Module `_netzwerk-geometry.js` (keine DOM-/D3-Aufrufe, testbar). Tab nicht mehr `hidden`, `'netzwerk'` in `VISIBLE_TABS`; Smoke-Erwartung `["total", "ring1", "ring2", "agrelon"]`. Präzisiert E-81 für den Netzwerk-Tab. |
| E-86 | Wissenskorb reaktiviert + an E-75/E-77-Designsprache angeglichen. `hidden` am Korb-Button raus, `VISIBLE_TABS` um `'korb'` ergaenzt, `updateKorbTabVisibility()` aufgeraeumt (setzte vorher `btn.hidden = false` bedingungslos und sabotierte damit E-81 fuer den Korb). Der Card-Renderer in `docs/js/views/korb.js` nutzt jetzt `buildRoleChip()` aus `archiv-inline-detail.js` und dieselbe funktionale Gruppierung (Produktion / Mitwirkende / Werk & Repertoire / Ort & Ereignis / Erwaehnt / Weitere / Beziehungen / Finanzen) wie das Inline-Detail — also Uppercase-Rolle-Prefix, Cluster-Farbe, Provenance-Pille, Wikidata-Badge, Index-Klick. CSV/BibTeX-Exporte tragen zusaetzliche Spalten `Beziehungen` (AgRelOn) und `Finanzen` (Betrag + Waehrung + Rolle); Orte werden aus `store.recordToEvents` gespeist, nicht nur aus `rico:hasOrHadLocation`; BibTeX-Author fällt bei fehlendem `verfasser:in` auf `agrelon:HasCorrespondent`-Sender zurueck. Begruendung: der Korb ist Werkzeug-Tab (E-80), nicht Perspektiv-Tab — er gehoert wieder in die Tab-Bar, ohne die Design-Einheit mit dem Bestand-Inline-Detail zu brechen. Die alte Typ-basierte Gruppierung (Personen / Institutionen / Orte / Werke / Rollen / Erwaehnt) entfiel; sie kollidierte mit der E-77-Umstellung auf funktionale Bloecke. Bookmark-Buttons in Bestand, Indizes und Inline-Detail sind strukturell unveraendert. |

## Offene Entscheidungen

| Thema | Prioritaet | Status |
|---|---|---|
| Matrix Zeitfilter UI | hoch | Slider, Dropdown, oder Timeline-Brush |
| ~~Wikidata in Kosmos-View~~ | ~~mittel~~ | erledigt: UA-Distanz-Annotation + Tooltip (E-60) |
| **SKOS-Labels aus Pipeline pflegen** | mittel | Aktuell sind `skos:prefLabel` der DFT-Concepts identisch mit der DFT-ID (z. B. `autobiografie` statt `Autobiografie`). Folge: Frontend kann die SKOS-Hierarchie nicht als Label-Quelle nutzen und behält eine Handtabelle (`DOKUMENTTYP_LABELS`) -- die driftet zwangslaeufig. Fix: Pipeline muss DFT-Concepts in `scripts/transform.py` mit lesbaren Labels ausstatten (z. B. aus Objekte-XLSX oder Hand-Map im Python-Teil), dann kann das Frontend blind vertrauen. |
| **AgRelOn-Granularitaet** | mittel | Aktuell wird bei Korrespondenz pauschal `agrelon:HasCorrespondent` erzeugt, und nur fuer die `adressat:in`-Rolle -- der Verfasser (= Absender) wird nicht in die AgRelOn-Relation aufgenommen. Folge: UI-Sektion „KORRESPONDENZ" zeigt nur einen Partner, der zweite steht unter „PRODUKTION VERFASSER". Optionen: (a) beide Richtungen erzeugen (symmetrisch), (b) feinere Praedikate `HasAddressee` / `HasSender` nutzen, (c) status quo belassen und Frontend beide Seiten zusammenfuehren. Entscheidung offen, Design-Input benoetigt. |

## Technische Schulden

- Kein JSON-LD/GEXF-Export aus Archiv-View (offen)
- Alle Iteration-1-Schulden behoben (monolithische Dateien, synthetische Daten, Inline-CSS)
- `aggregator.js` dupliziert Pipeline-Logik (pre-built JSONs nutzen 5-Jahres-Perioden statt Lebensphasen → Pipeline-Update noetig)
- Legend-Builder nicht konsolidiert (bewusst: zu heterogene Patterns, Abstraktion lohnt nicht)

## Verschobene Features

| Prioritaet | Feature | Status |
|---|---|---|
| Hoch | Export CSV/JSON-LD/GEXF | CSV/BibTeX erledigt (Session 20), JSON-LD/GEXF offen |
| Mittel | Matrix Zeitfilter/Zoom | offen |
| ~~Mittel~~ | ~~Cross-Visualization Linking~~ | erledigt (Session 20): Matrix ↔ Kosmos ↔ Indizes |
| ~~Mittel~~ | ~~Merkliste + CSV-Export~~ | erledigt (Session 20): Wissenskorb + CSV/BibTeX |
| Niedrig | Leaflet Karte | offen |

## Prozesswissen

### Was funktioniert hat

- Promptotyping-Dokumente als Source of Truth → Code-Generierung
- Synthetische Daten entkoppeln Frontend- von Datenarbeit
- Design-System als CSS Custom Properties vorab definiert
- Offline-first ueberlebt Funding-Gaps
- Iterative Vis-Entwicklung (Partitur → Patterns fuer Matrix/Kosmos)

### Iteration-2-Erkenntnisse

- Data-first statt UI-first
- Modularisierung von Anfang an
- User Testing frueher
- Evaluation-driven Priorisierung (schwach abgedeckte Forschungsfragen früh benennen)
- Controlled Vocabulary Enforcement bei Datenerfassung

### Positive Ueberraschungen aus Datenanalyse

- Erschliessungstiefe bei den feinerschlossenen Konvoluten (NIM_003/004/005/006/007) uebertrifft Erwartungen
- Gender-inklusives Rollen-Vokabular mit substanziellem `:in`-Anteil
- Hoher Personen-Kategorien-Abdeckungsgrad — Matrix bekommt direkt Daten
- Werk-Verknuepfungen ermoeglichen substantiellen Rollen-Kosmos
