[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: tests that re-run the pipeline (use -m "not slow" to skip)
    frontend: headless-browser smoke tests (start HTTP server first; run with `pytest -m frontend`)
filterwarnings =
    ignore::DeprecationWarning
